import styled from "styled-components";

const SectionStyled = styled.section`
  min-width: 400px;
  margin: 0 auto;
  text-align: center;

  .sectionTitle {
    font-size: 40px;
  }
`;

export default SectionStyled;
